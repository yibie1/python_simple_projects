# text = """ first 
# second  
#  third"""
# print(text)
#         ..... 1 multiline in triple cote 

# note : we can multiply strings by number eg. text = 2 * 'abc'
#      : sbustr(for select strings range) eg. text[1:3], text[:2], text[2:] slice instade of substr
#       : in python strings immutable(i.e we can't change them)
#       : .upper(for upper case the string) .lower (for lower case the string)

#  in this example how to change text by using substr

# text = 'abcd'
# print(text[:2] + 'y' + text[3:])   #in this  exampel we replace 'c' in 'y'

# index of string eg. print(text['a']) // the output is the index of a
